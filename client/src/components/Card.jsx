import React from 'react'

const Card = ({user}) => {
  return (
    <div className=''>
      <h5>{user.heading}</h5>
      <h5>{user.number}</h5>
      <h5>{user.dollar}</h5>

    
    in last month

    </div>
  )
}

export default Card
